using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using Aki.Config;
using Aki.Protocol;
using Aki.TDConfigMgr.AutoGenerated.IEntity;
using Aki.TDConfigMgr.Component;
using CSharpScript.Core.Common;
using CSharpScript.Game.Utils.CombatStateMachine;

// Token: 0x02000CE9 RID: 3305
[NullableContext(1)]
[Nullable(new byte[]
{
	0,
	1
})]
[Config(0)]
public class AiConfig : ConfigBase<AiConfig>
{
	// Token: 0x060041BB RID: 16827 RVA: 0x0006D960 File Offset: 0x0006BB60
	protected override bool OnInit()
	{
		AiStateMachineConfig? config = ConfigAiStateMachineConfigById.GetConfig("SM_Common", true);
		if (config == null)
		{
			Singleton<Log>.Instance.Error(ELogModule.BehaviorTree, ELogAuthor.MZJ, "AiConfig Init fail, no commonStateMachine config", default(ReadOnlySpan<ValueTuple<string, object>>));
			return false;
		}
		this.CommonStateMachineJsonObject = Json.Decode<CombatStateMachineDefine.Fsm.StateMachineGroup>(config.Value.StateMachineJson, null);
		foreach (BlackboardWhiteList blackboardWhiteList in ConfigBlackboardWhiteListAll.GetConfigList(true))
		{
			this.BlackboardWhiteList.Add(blackboardWhiteList.Key);
		}
		return true;
	}

	// Token: 0x060041BC RID: 16828 RVA: 0x0006DA0C File Offset: 0x0006BC0C
	protected override bool OnClear()
	{
		this.BlackboardWhiteList.Clear();
		return true;
	}

	// Token: 0x060041BD RID: 16829 RVA: 0x0006DA1A File Offset: 0x0006BC1A
	public bool CheckBlackboardWhiteList(string key)
	{
		return this.BlackboardWhiteList.Contains(key);
	}

	// Token: 0x060041BE RID: 16830 RVA: 0x0006DA28 File Offset: 0x0006BC28
	public unsafe void LoadAiConfig(AiController aiController, int aiBaseId, bool isNpc = false)
	{
		CharacterActorComponent charActorComp = aiController.CharActorComp;
		aiController.AiBase = ConfigAiBaseById.GetConfig(aiBaseId, true);
		if (aiController.AiBase == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiBase配置";
			<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray3<ValueTuple<string, object>>);
			*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("Id", aiBaseId);
			*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
			*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
			instance.Error(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 3));
			return;
		}
		if (aiController.AiBase.Value.SubBehaviorConfigsLength > 0)
		{
			this.LoadAiSubConfigs(aiController, isNpc);
		}
		if (!string.IsNullOrEmpty(aiController.AiBase.Value.StateMachine))
		{
			aiController.StateMachineConfig = ConfigAiStateMachineConfigById.GetConfig(aiController.AiBase.Value.StateMachine, true);
		}
	}

	// Token: 0x060041BF RID: 16831 RVA: 0x0006DB3C File Offset: 0x0006BD3C
	private unsafe void LoadAiWanderBodyConfig(AiController aiController)
	{
		CharacterActorComponent charActorComp = aiController.CharActorComp;
		CreatureDataComponent creatureData = charActorComp.CreatureData;
		IEntityData entityData = (creatureData != null) ? creatureData.GetPbEntityInitData() : null;
		if (entityData == null)
		{
			return;
		}
		AiComponent component = TdUtils.GetComponent<AiComponent>(entityData.ComponentsData, EConfigComponent.AiComponent);
		if (((component != null) ? component.InitState : null) == null)
		{
			return;
		}
		if (component.InitState.Type != EInitStateType.Standby)
		{
			return;
		}
		IInitStateStandby initStateStandby = (IInitStateStandby)component.InitState;
		if (initStateStandby.Wander != null)
		{
			EAiWanderType? wander = initStateStandby.Wander;
			EAiWanderType eaiWanderType = (EAiWanderType)0;
			if (!(wander.GetValueOrDefault() == eaiWanderType & wander != null))
			{
				if (aiController.AiWanderRadiusConfig == null)
				{
					aiController.AiWanderRadiusConfig = ConfigAiWanderRadiusConfigById.GetConfig((int)initStateStandby.Wander.Value, true);
					if (aiController.AiWanderRadiusConfig == null)
					{
						Log instance = Singleton<Log>.Instance;
						ELogModule module = ELogModule.BehaviorTree;
						ELogAuthor author = ELogAuthor.LJM;
						string message = "缺少AiWander的范围配置";
						<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray3<ValueTuple<string, object>>);
						*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("Id", initStateStandby.Wander);
						*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
						*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
						instance.Warn(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 3));
					}
				}
				return;
			}
		}
	}

	// Token: 0x060041C0 RID: 16832 RVA: 0x0006DC9C File Offset: 0x0006BE9C
	private unsafe void LoadAiSubConfigs(AiController aiController, bool isNpc = false)
	{
		CharacterActorComponent charActorComp = aiController.CharActorComp;
		SpecialHateAndSense? specialConfig = this.LoadSpecialHateAndSenseConfig(aiController);
		CharacterActorComponent charActorComp2 = aiController.CharActorComp;
		EntityComponentPb entityComponentPb;
		MonsterAiComponentPb monsterAiComponentPb = (charActorComp2 != null && charActorComp2.CreatureData.ComponentDataMap.TryGetValue("MonsterAiComponentPb", out entityComponentPb)) ? ((entityComponentPb != null) ? entityComponentPb.MonsterAiComponentPb : null) : null;
		this.LoadAiWanderBodyConfig(aiController);
		if (aiController.AiBase == null || aiController.AiBase.Value.SubBehaviorConfigsLength == 0)
		{
			return;
		}
		string subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiWander");
		if (subBehaviorConfigs != null)
		{
			if (aiController.AiWanderInfos == null)
			{
				aiController.AiWanderInfos = new AiWanderInfos();
			}
			int num;
			int p0Id = int.TryParse(subBehaviorConfigs, out num) ? num : 0;
			aiController.AiWanderInfos.AiWander = ConfigAiWanderById.GetConfig(p0Id, true);
			if (aiController.AiWanderInfos.AiWander == null)
			{
				Log instance = Singleton<Log>.Instance;
				ELogModule module = ELogModule.BehaviorTree;
				ELogAuthor author = ELogAuthor.LCZ;
				string message = "缺少AiWander配置";
				<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray3<ValueTuple<string, object>>);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("Id", subBehaviorConfigs);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
				instance.Error(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 3));
			}
		}
		subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiBattleWander");
		if (subBehaviorConfigs != null)
		{
			if (aiController.AiWanderInfos == null)
			{
				aiController.AiWanderInfos = new AiWanderInfos();
			}
			int num2;
			AiBattleWander? config = ConfigAiBattleWanderById.GetConfig(int.TryParse(subBehaviorConfigs, out num2) ? num2 : 0, true);
			if (config == null)
			{
				Log instance2 = Singleton<Log>.Instance;
				ELogModule module2 = ELogModule.BehaviorTree;
				ELogAuthor author2 = ELogAuthor.LCZ;
				string message2 = "缺少AiBattleWander配置";
				<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray2 = default(<>y__InlineArray3<ValueTuple<string, object>>);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray2, 0) = new ValueTuple<string, object>("Id", subBehaviorConfigs);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray2, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray2, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
				instance2.Error(module2, author2, message2, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray2, 3));
			}
			else if (config.Value.GroupIdsLength == 0)
			{
				aiController.AiWanderInfos.AiBattleWanderGroups = new List<AiBattleWanderGroup>();
			}
			else
			{
				List<AiBattleWanderGroup> list = new List<AiBattleWanderGroup>();
				int[] array = config.Value.GroupIds();
				for (int i = 0; i < array.Length; i++)
				{
					list.Add(ConfigAiBattleWanderGroupById.GetConfig(array[i], true).Value);
				}
				Dictionary<int, AiBattleWanderGroup> dictionary = new Dictionary<int, AiBattleWanderGroup>();
				foreach (AiBattleWanderGroup value in list)
				{
					dictionary[value.Id] = value;
				}
				list.Clear();
				foreach (int key in config.Value.GroupIds())
				{
					list.Add(dictionary[key]);
				}
				aiController.AiWanderInfos.AiBattleWanderGroups = list;
			}
		}
		subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiBaseSkill");
		if (subBehaviorConfigs != null)
		{
			aiController.AiSkill = new AiSkill(aiController);
			int num3;
			int p0Id2 = int.TryParse(subBehaviorConfigs, out num3) ? num3 : 0;
			aiController.AiSkill.BaseSkill = ConfigAiBaseSkillById.GetConfig(p0Id2, true);
			if (aiController.AiSkill.BaseSkill == null)
			{
				Log instance3 = Singleton<Log>.Instance;
				ELogModule module3 = ELogModule.BehaviorTree;
				ELogAuthor author3 = ELogAuthor.LCZ;
				string message3 = "缺少AiBaseSkill配置";
				<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray3 = default(<>y__InlineArray3<ValueTuple<string, object>>);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray3, 0) = new ValueTuple<string, object>("Id", subBehaviorConfigs);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray3, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray3, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
				instance3.Error(module3, author3, message3, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray3, 3));
			}
			else
			{
				this.LoadAiSkillDetails(aiController.AiSkill);
			}
		}
		aiController.AiHateList.AiHate = this.LoadAiHateByController(aiController, specialConfig, (monsterAiComponentPb != null) ? new long?(monsterAiComponentPb.HatredId) : null);
		subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiAlert");
		aiController.AiAlert.AiAlertConfig = this.LoadAiAlert(subBehaviorConfigs);
		if (!isNpc || aiController.AiAlert.AiAlertConfig != null)
		{
			subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiSense");
			aiController.AiPerception = this.LoadAiSenseConfigCreateAiPerception(aiController, (monsterAiComponentPb != null) ? monsterAiComponentPb.BasicPerceptionIds : null, subBehaviorConfigs, specialConfig);
		}
		subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiPatrol");
		bool flag = ControllerBase<MonsterGroupPatrolController>.Instance.IsEntityInGroupPatrol(charActorComp.Entity);
		if (!string.IsNullOrEmpty(subBehaviorConfigs) && !flag)
		{
			int num4;
			AiPatrol? config2 = ConfigAiPatrolById.GetConfig(int.TryParse(subBehaviorConfigs.Split('|', StringSplitOptions.None)[0], out num4) ? num4 : 0, true);
			aiController.AiPatrol.ResetConfig(config2);
			if (config2 == null)
			{
				Log instance4 = Singleton<Log>.Instance;
				ELogModule module4 = ELogModule.BehaviorTree;
				ELogAuthor author4 = ELogAuthor.LCZ;
				string message4 = "缺少AiPatrol配置";
				<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray4 = default(<>y__InlineArray3<ValueTuple<string, object>>);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray4, 0) = new ValueTuple<string, object>("Id", subBehaviorConfigs);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray4, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray4, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
				instance4.Error(module4, author4, message4, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray4, 3));
			}
		}
		subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiFlee");
		if (subBehaviorConfigs != null)
		{
			int num5;
			int p0Id3 = int.TryParse(subBehaviorConfigs, out num5) ? num5 : 0;
			aiController.AiFlee = ConfigAiFleeById.GetConfig(p0Id3, true);
			if (aiController.AiFlee == null)
			{
				Log instance5 = Singleton<Log>.Instance;
				ELogModule module5 = ELogModule.BehaviorTree;
				ELogAuthor author5 = ELogAuthor.LCZ;
				string message5 = "缺少AiFlee配置";
				<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray5 = default(<>y__InlineArray3<ValueTuple<string, object>>);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray5, 0) = new ValueTuple<string, object>("Id", subBehaviorConfigs);
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray5, 1) = new ValueTuple<string, object>("Target", charActorComp.Actor.GetName());
				*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray5, 2) = new ValueTuple<string, object>("CreatureId", charActorComp.CreatureData.GetOwnerId());
				instance5.Error(module5, author5, message5, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray5, 3));
			}
		}
	}

	// Token: 0x060041C1 RID: 16833 RVA: 0x0006E37C File Offset: 0x0006C57C
	private void LoadAiSkillDetails(AiSkill aiSkill)
	{
		if (aiSkill.BaseSkill.Value.RandomSkillsLength > 0)
		{
			aiSkill.ActiveSkillGroup.Add(0);
		}
		HashSet<int> hashSet = new HashSet<int>();
		List<AiSkillInfos> list = new List<AiSkillInfos>();
		foreach (IntArray intArray in aiSkill.BaseSkill.Value.RandomSkills())
		{
			foreach (int num in intArray.ArrayInt())
			{
				if (!hashSet.Contains(num))
				{
					hashSet.Add(num);
					list.Add(ConfigAiSkillInfosById.GetConfig(num, true).Value);
				}
			}
		}
		hashSet.Clear();
		List<AiSkillPrecondition> list2 = new List<AiSkillPrecondition>();
		foreach (AiSkillInfos value in list)
		{
			aiSkill.SkillInfos[value.Id] = value;
			if (!hashSet.Contains(value.SkillPreconditionId))
			{
				hashSet.Add(value.SkillPreconditionId);
				list2.Add(ConfigAiSkillPreconditionById.GetConfig(value.SkillPreconditionId, true).Value);
			}
		}
		foreach (AiSkillPrecondition value2 in list2)
		{
			aiSkill.SkillPreconditionMap[value2.Id] = value2;
		}
		aiSkill.InitTagMap();
	}

	// Token: 0x060041C2 RID: 16834 RVA: 0x0006E520 File Offset: 0x0006C720
	public unsafe void LoadAiTeamConfigNew(AiTeam aiTeam, int aiTeamLevelId)
	{
		aiTeam.AiTeamLevel = ConfigAiTeamLevelNewById.GetConfig(aiTeamLevelId, true);
		if (aiTeam.AiTeamLevel == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.AI;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "没有配置AiTeamLevel";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("Id", aiTeamLevelId);
			instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return;
		}
		aiTeam.AiTeamAreas = new List<AiTeamAreaNew>();
		int[] array = aiTeam.AiTeamLevel.Value.PositionId();
		for (int i = 0; i < array.Length; i++)
		{
			AiTeamAreaNew? config = ConfigAiTeamAreaNewById.GetConfig(array[i], true);
			aiTeam.AiTeamAreas.Add(config.Value);
		}
		aiTeam.AiTeamAttacks = new List<AiTeamAttack>();
		Dictionary<int, AiTeamAttack> dictionary = new Dictionary<int, AiTeamAttack>();
		foreach (AiTeamAreaNew aiTeamAreaNew in aiTeam.AiTeamAreas)
		{
			AiTeamAttack item;
			if (dictionary.TryGetValue(aiTeamAreaNew.AttackWeightId, out item))
			{
				aiTeam.AiTeamAttacks.Add(item);
			}
			else
			{
				AiTeamAttack? config2 = ConfigAiTeamAttackById.GetConfig(aiTeamAreaNew.AttackWeightId, true);
				if (config2 == null)
				{
					Log instance2 = Singleton<Log>.Instance;
					ELogModule module2 = ELogModule.BehaviorTree;
					ELogAuthor author2 = ELogAuthor.LCZ;
					string message2 = "没有配置AiTeamAttack";
					<>y__InlineArray2<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray2<ValueTuple<string, object>>);
					*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("AttackWeightId", aiTeamAreaNew.AttackWeightId);
					*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("LocationId", aiTeamAreaNew.Id);
					instance2.Error(module2, author2, message2, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 2));
				}
				else
				{
					dictionary[aiTeamAreaNew.AttackWeightId] = config2.Value;
					aiTeam.AiTeamAttacks.Add(config2.Value);
				}
			}
		}
	}

	// Token: 0x060041C3 RID: 16835 RVA: 0x0006E6E8 File Offset: 0x0006C8E8
	public AiPatrol? LoadAiPatrolConfig(AiBase? aiBase, int patrolIndex)
	{
		if (aiBase == null || aiBase.Value.SubBehaviorConfigsLength == 0)
		{
			return null;
		}
		string subBehaviorConfigs = aiBase.Value.GetSubBehaviorConfigs("AiPatrol");
		if (subBehaviorConfigs == null || string.IsNullOrEmpty(subBehaviorConfigs))
		{
			return null;
		}
		string[] array = subBehaviorConfigs.Split('|', StringSplitOptions.None);
		if (array.Length <= patrolIndex)
		{
			return null;
		}
		int num;
		AiPatrol? config = ConfigAiPatrolById.GetConfig(int.TryParse(array[patrolIndex], out num) ? num : 0, true);
		if (config == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiPatrol配置";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("Id", array[patrolIndex]);
			instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return null;
		}
		return config;
	}

	// Token: 0x060041C4 RID: 16836 RVA: 0x0006E7B8 File Offset: 0x0006C9B8
	public AiPatrol? LoadAiPatrolConfigById(int patrolId)
	{
		AiPatrol? config = ConfigAiPatrolById.GetConfig(patrolId, true);
		if (config == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiPatrol配置";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("Id", patrolId);
			instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return null;
		}
		return config;
	}

	// Token: 0x060041C5 RID: 16837 RVA: 0x0006E80C File Offset: 0x0006CA0C
	[NullableContext(2)]
	private unsafe IAiPerception LoadAiSenseConfigCreateAiPerception([Nullable(1)] AiController aiController, IReadOnlyList<int> protoSenseGroup, string configId, SpecialHateAndSense? specialConfig)
	{
		if (string.IsNullOrEmpty(configId))
		{
			return null;
		}
		int num;
		AiSenseGroup? config = ConfigAiSenseGroupById.GetConfig(int.TryParse(configId, out num) ? num : 0, true);
		if (config == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiSenseGroup配置";
			<>y__InlineArray2<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray2<ValueTuple<string, object>>);
			*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("Id", configId);
			*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("Actor", aiController.CharActorComp.Actor.GetName());
			instance.Error(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 2));
			return null;
		}
		bool flag = protoSenseGroup != null && protoSenseGroup.Count > 0;
		IEnumerable<int> enumerable;
		if (!flag)
		{
			IReadOnlyList<int> readOnlyList = config.Value.AiSenseIds();
			enumerable = readOnlyList;
		}
		else
		{
			enumerable = protoSenseGroup;
		}
		List<AiSense> list = new List<AiSense>();
		int num2 = 0;
		foreach (int num3 in enumerable)
		{
			int p0Id;
			if (!flag && num2 == 0 && specialConfig != null)
			{
				p0Id = specialConfig.Value.FirstAiSenseId;
			}
			else
			{
				p0Id = num3;
			}
			list.Add(ConfigAiSenseById.GetConfig(p0Id, true).Value);
			num2++;
		}
		if (this.AsyncAiPerception)
		{
			return new AsyncAiPerception(aiController, config.Value, list.ToArray());
		}
		return new AiPerception(aiController, config.Value, list.ToArray());
	}

	// Token: 0x060041C6 RID: 16838 RVA: 0x0006E988 File Offset: 0x0006CB88
	public AiSense? LoadAiSense(string aiSenseId)
	{
		int num;
		AiSense? config = ConfigAiSenseById.GetConfig(int.TryParse(aiSenseId, out num) ? num : 0, true);
		if (config == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiSense配置";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("AiSenseId", aiSenseId);
			instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return null;
		}
		return config;
	}

	// Token: 0x060041C7 RID: 16839 RVA: 0x0006E9E4 File Offset: 0x0006CBE4
	public SpecialHateAndSense? LoadSpecialHateAndSenseConfig(AiController aiController)
	{
		MonsterComponent monsterComponent = aiController.CharActorComp.Entity.GetComponent<CreatureDataComponent>().GetMonsterComponent();
		SpecialHateAndSense? result = null;
		if (monsterComponent != null && monsterComponent.SpecialHateAndSenseConfig != null)
		{
			int? specialHateAndSenseConfig = monsterComponent.SpecialHateAndSenseConfig;
			int num = 0;
			if (!(specialHateAndSenseConfig.GetValueOrDefault() == num & specialHateAndSenseConfig != null))
			{
				result = ConfigSpecialHateAndSenseById.GetConfig(monsterComponent.SpecialHateAndSenseConfig.Value, true);
				if (result == null)
				{
					Log instance = Singleton<Log>.Instance;
					ELogModule module = ELogModule.BehaviorTree;
					ELogAuthor author = ELogAuthor.LCZ;
					string message = "缺少SpecialHateAndSense配置";
					ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("Id", monsterComponent.SpecialHateAndSenseConfig);
					instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
				}
			}
		}
		return result;
	}

	// Token: 0x060041C8 RID: 16840 RVA: 0x0006EA90 File Offset: 0x0006CC90
	public AiHate? LoadAiHate(int aiHateId)
	{
		if (aiHateId == 0)
		{
			return null;
		}
		AiHate? config = ConfigAiHateById.GetConfig(aiHateId, true);
		if (config == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiHate配置";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("Id", aiHateId);
			instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
		}
		return config;
	}

	// Token: 0x060041C9 RID: 16841 RVA: 0x0006EAE8 File Offset: 0x0006CCE8
	public AiHate? LoadAiHateByController(AiController aiController, SpecialHateAndSense? specialConfig, long? hatredId = null)
	{
		if (hatredId != null)
		{
			long? num = hatredId;
			long num2 = 0L;
			if (num.GetValueOrDefault() > num2 & num != null)
			{
				return this.LoadAiHate((int)hatredId.Value);
			}
		}
		SpecialHateAndSense? specialHateAndSense = specialConfig;
		if (specialHateAndSense == null)
		{
			specialHateAndSense = this.LoadSpecialHateAndSenseConfig(aiController);
		}
		string subBehaviorConfigs = aiController.AiBase.Value.GetSubBehaviorConfigs("AiHate");
		int num3;
		int aiHateId = int.TryParse((specialHateAndSense != null) ? specialHateAndSense.Value.AiHateId.ToString() : subBehaviorConfigs, out num3) ? num3 : 0;
		return this.LoadAiHate(aiHateId);
	}

	// Token: 0x060041CA RID: 16842 RVA: 0x0006EB90 File Offset: 0x0006CD90
	[NullableContext(2)]
	public AiAlert? LoadAiAlert(string aiAlertId)
	{
		if (string.IsNullOrEmpty(aiAlertId))
		{
			return null;
		}
		int num;
		AiAlert? config = ConfigAiAlertById.GetConfig(int.TryParse(aiAlertId, out num) ? num : 0, true);
		if (config == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.BehaviorTree;
			ELogAuthor author = ELogAuthor.LCZ;
			string message = "缺少AiAlert配置";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("Id", aiAlertId);
			instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
		}
		return config;
	}

	// Token: 0x0400101B RID: 4123
	private const string commonStateMachine = "SM_Common";

	// Token: 0x0400101C RID: 4124
	[Nullable(2)]
	public CombatStateMachineDefine.Fsm.StateMachineGroup CommonStateMachineJsonObject;

	// Token: 0x0400101D RID: 4125
	private readonly HashSet<string> BlackboardWhiteList = new HashSet<string>();

	// Token: 0x0400101E RID: 4126
	public bool AsyncAiPerception;

	// Token: 0x0400101F RID: 4127
	public bool CppAsyncAiPerception;
}
