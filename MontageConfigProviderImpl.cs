using System;
using System.Runtime.CompilerServices;
using Aki.TDConfigMgr.AutoGenerated.IFlow;
using CSharpScript.Game.LevelGamePlay.LevelAi.BehaviorTree;
using CSharpScript.Game.Module.Plot;

// Token: 0x02000F9B RID: 3995
public class MontageConfigProviderImpl : IMontageConfigProvider
{
	// Token: 0x060065EF RID: 26095 RVA: 0x0019AFA8 File Offset: 0x001991A8
	[NullableContext(2)]
	public IMontageConfig GetMontageConfig(int montageId, bool isOverlay, bool isAbp)
	{
		PlotModel instance = ModelBase<PlotModel>.Instance;
		if (instance == null)
		{
			return null;
		}
		if (isOverlay)
		{
			return instance.GetOverlayAbpMontageConfig(montageId, false);
		}
		if (isAbp)
		{
			return instance.GetAbpMontageConfig(montageId, false);
		}
		return instance.GetMontageConfig(montageId, false);
	}
}
