using System;
using System.Runtime.CompilerServices;
using Aki.Config;
using Aki.TDConfigMgr.AutoGenerated.IVar;

// Token: 0x02001418 RID: 5144
[Nullable(new byte[]
{
	0,
	1
})]
[Model(0)]
public class MoonChasingTaskModel : ModelBase<MoonChasingTaskModel>
{
	// Token: 0x06008E9A RID: 36506 RVA: 0x00257374 File Offset: 0x00255574
	public EQuestState GetBranchLineState(int branchLineId)
	{
		BranchLine? branchLineTaskById = ConfigBase<TaskConfig>.Instance.GetBranchLineTaskById(branchLineId);
		if (branchLineTaskById == null)
		{
			return EQuestState.InActive;
		}
		return ModelBase<QuestNewModel>.Instance.GetQuestState(branchLineTaskById.Value.TaskId);
	}

	// Token: 0x06008E9B RID: 36507 RVA: 0x002573B4 File Offset: 0x002555B4
	public EQuestState GetMainLineState(int mainLineId)
	{
		MainLine? mainLineTaskById = ConfigBase<TaskConfig>.Instance.GetMainLineTaskById(mainLineId);
		if (mainLineTaskById == null)
		{
			return EQuestState.InActive;
		}
		return ModelBase<QuestNewModel>.Instance.GetQuestState(mainLineTaskById.Value.TaskId);
	}

	// Token: 0x06008E9C RID: 36508 RVA: 0x002573F4 File Offset: 0x002555F4
	public int GetFirstReadyBranchTask()
	{
		foreach (BranchLine branchLine in ConfigBase<TaskConfig>.Instance.GetAllBranchLineTask())
		{
			EQuestState questState = ModelBase<QuestNewModel>.Instance.GetQuestState(branchLine.TaskId);
			if (questState == EQuestState.Ready || questState == EQuestState.Progress)
			{
				return branchLine.Id;
			}
		}
		return 0;
	}
}
