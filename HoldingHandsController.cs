using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using Aki.Protocol;
using Aki.TDConfigMgr.AutoGenerated.IEntity;
using Aki.TDConfigMgr.Component;
using AkiClient.Game.Aki.Data.Level.KeepFollowing;
using CSharpScript.Game.Module.Plot;
using CSharpScript.Game.NewWorld.Pawn.Component;
using UnrealEngine;

// Token: 0x02001EC4 RID: 7876
[NullableContext(1)]
[Nullable(new byte[]
{
	0,
	1
})]
[Controller(0)]
public class HoldingHandsController : ControllerBase<HoldingHandsController>
{
	// Token: 0x170011D7 RID: 4567
	// (get) Token: 0x0600E8E6 RID: 59622 RVA: 0x003F013E File Offset: 0x003EE33E
	[Nullable(2)]
	public HoldingHandsModel Model
	{
		[NullableContext(2)]
		get
		{
			return ModelBase<HoldingHandsModel>.Instance;
		}
	}

	// Token: 0x0600E8E7 RID: 59623 RVA: 0x003F0145 File Offset: 0x003EE345
	protected override bool OnInit()
	{
		Singleton<Net>.Instance.Register<StopHoldHandNotify>(ENotifyMessageId.StopHoldHandNotify, new Action<StopHoldHandNotify, Net.CallbackStatus>(this.OnStopHoldHandNotify));
		return true;
	}

	// Token: 0x0600E8E8 RID: 59624 RVA: 0x003F0164 File Offset: 0x003EE364
	protected override bool OnClear()
	{
		Singleton<Net>.Instance.UnRegister(ENotifyMessageId.StopHoldHandNotify);
		return true;
	}

	// Token: 0x0600E8E9 RID: 59625 RVA: 0x003F0177 File Offset: 0x003EE377
	public void TickPriority2(double deltaTime)
	{
		this.TickBindings(deltaTime);
	}

	// Token: 0x0600E8EA RID: 59626 RVA: 0x003F0180 File Offset: 0x003EE380
	private unsafe void TickBindings(double deltaTime)
	{
		if (this.Model == null)
		{
			return;
		}
		foreach (HoldingHandsRelation holdingHandsRelation in this.Model.Relations.Values)
		{
			Binding binding = holdingHandsRelation as Binding;
			if (binding != null)
			{
				Singleton<HoldingHandsUtils>.Instance.UpdateBinding(binding, deltaTime);
				if (binding.Reachable && !binding.LastReachable)
				{
					Log instance = Singleton<Log>.Instance;
					ELogModule module = ELogModule.Character;
					ELogAuthor author = ELogAuthor.LJF;
					string message = "[HoldingHandsController] 牵手范围内";
					<>y__InlineArray2<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray2<ValueTuple<string, object>>);
					ref ValueTuple<string, object> ptr = ref <PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0);
					string item = "leader";
					CharacterHoldingHandsComponent leader = binding.Leader;
					ptr = new ValueTuple<string, object>(item, (leader != null) ? new int?(leader.Entity.Id) : null);
					ref ValueTuple<string, object> ptr2 = ref <PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1);
					string item2 = "follower";
					CharacterHoldingHandsComponent follower = binding.Follower;
					ptr2 = new ValueTuple<string, object>(item2, (follower != null) ? new int?(follower.Entity.Id) : null);
					instance.Info(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 2));
					binding.Leader.OnReachable(true, binding.LeaderHandType);
					binding.Follower.OnReachable(true, binding.FollowerHandType);
				}
				else if (!binding.Reachable && binding.LastReachable)
				{
					Log instance2 = Singleton<Log>.Instance;
					ELogModule module2 = ELogModule.Character;
					ELogAuthor author2 = ELogAuthor.LJF;
					string message2 = "[HoldingHandsController] 牵手范围外";
					<>y__InlineArray2<ValueTuple<string, object>> <>y__InlineArray2 = default(<>y__InlineArray2<ValueTuple<string, object>>);
					ref ValueTuple<string, object> ptr3 = ref <PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray2, 0);
					string item3 = "leader";
					CharacterHoldingHandsComponent leader2 = binding.Leader;
					ptr3 = new ValueTuple<string, object>(item3, (leader2 != null) ? new int?(leader2.Entity.Id) : null);
					ref ValueTuple<string, object> ptr4 = ref <PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray2, 1);
					string item4 = "follower";
					CharacterHoldingHandsComponent follower2 = binding.Follower;
					ptr4 = new ValueTuple<string, object>(item4, (follower2 != null) ? new int?(follower2.Entity.Id) : null);
					instance2.Info(module2, author2, message2, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray2, 2));
					binding.Leader.OnReachable(false, binding.LeaderHandType);
					binding.Follower.OnReachable(false, binding.FollowerHandType);
				}
				if (binding.UnReachableTime > binding.Leader.Params.EndTime * 1000.0)
				{
					Log instance3 = Singleton<Log>.Instance;
					ELogModule module3 = ELogModule.Character;
					ELogAuthor author3 = ELogAuthor.LJF;
					string message3 = "[HoldingHandsController] 因范围外超时断开牵手";
					<>y__InlineArray2<ValueTuple<string, object>> <>y__InlineArray3 = default(<>y__InlineArray2<ValueTuple<string, object>>);
					*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray3, 0) = new ValueTuple<string, object>("entityId", binding.Leader.Entity.Id);
					*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray3, 1) = new ValueTuple<string, object>("key", binding.Key);
					instance3.Info(module3, author3, message3, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray2<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray3, 2));
					this.RequestReleaseHands(binding.Key, "超时终止", true, true);
				}
			}
		}
	}

	// Token: 0x0600E8EB RID: 59627 RVA: 0x003F0488 File Offset: 0x003EE688
	private void AddInvitation(string key, CharacterHoldingHandsComponent leader, CharacterHoldingHandsComponent follower, EHandType leaderHandType)
	{
		Invitation invitation = new Invitation();
		invitation.Key = key;
		invitation.Leader = leader;
		invitation.LeaderHandType = leaderHandType;
		invitation.Follower = follower;
		invitation.FollowerHandType = EHandType.Right - (int)leaderHandType;
		HoldingHandsModel model = this.Model;
		if (model != null)
		{
			model.SetRelation(key, invitation);
		}
		CharacterHoldingHandsComponent.StartInvitation(invitation, leaderHandType);
		invitation.Leader.TryAddEvents();
		invitation.Follower.TryAddEvents();
	}

	// Token: 0x0600E8EC RID: 59628 RVA: 0x003F04F4 File Offset: 0x003EE6F4
	public unsafe Binding AddBinding(string key, CharacterHoldingHandsComponent leader, CharacterHoldingHandsComponent follower, EHandType leaderHandType, bool invitation = false, bool slowDamping = false)
	{
		Log instance = Singleton<Log>.Instance;
		ELogModule module = ELogModule.Character;
		ELogAuthor author = ELogAuthor.LJF;
		string message = "[HoldingHandController.AddBinding]";
		<>y__InlineArray5<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray5<ValueTuple<string, object>>);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("key", key);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("Leader", leader.Entity.Id);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("Follower", follower.Entity.Id);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 3) = new ValueTuple<string, object>("invitation", invitation);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 4) = new ValueTuple<string, object>("slowDamping", slowDamping);
		instance.Info(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 5));
		EHandType ehandType = EHandType.Right - (int)leaderHandType;
		HandRuntime handRuntime = leader.GetHandRuntime(leaderHandType);
		HandRuntime handRuntime2 = follower.GetHandRuntime(ehandType);
		Binding binding = new Binding();
		binding.Key = key;
		binding.LeaderHandType = leaderHandType;
		binding.FollowerHandType = ehandType;
		binding.Leader = leader;
		binding.LeaderRuntime = handRuntime;
		binding.Follower = follower;
		binding.FollowerRuntime = handRuntime2;
		binding.State = EBindingState.Logic;
		binding.BindDirDamping = (slowDamping ? leader.Params.BindVecDampingInvitation : leader.Params.BindVecDamping);
		handRuntime.IkTarget.Alpha = 1.0;
		handRuntime2.IkTarget.Alpha = 1.0;
		HoldingHandsModel model = this.Model;
		if (model != null)
		{
			model.SetRelation(key, binding);
		}
		leader.OnAddBinding(leaderHandType, key, invitation);
		follower.OnAddBinding(ehandType, key, invitation);
		if (!invitation && follower.KeepFollowingConfig != null)
		{
			BaseMoveComponent moveComp = follower.MoveComp;
			if (moveComp != null)
			{
				moveComp.MoveController.StartKeepFollowingWithDataAsset(leader.ActorComp, follower.KeepFollowingConfig, new EHandType?(ehandType), false, null, null);
			}
		}
		if (UKuroRenderingRuntimeBPPluginBPLibrary.GetWorldType(leader.SkelMesh.GetOwner()) == BP_EWorldType.PIE)
		{
			CharacterHoldingHandsComponent.LoadCommonParams();
		}
		return binding;
	}

	// Token: 0x0600E8ED RID: 59629 RVA: 0x003F06E8 File Offset: 0x003EE8E8
	private void DeleteRelation(string key, bool lerp = false)
	{
		HoldingHandsModel model = this.Model;
		HoldingHandsRelation holdingHandsRelation = (model != null) ? model.GetRelation(key) : null;
		if (holdingHandsRelation != null)
		{
			CharacterHoldingHandsComponent leader = holdingHandsRelation.Leader;
			if (leader != null)
			{
				leader.OnDeleteRelation(holdingHandsRelation.LeaderHandType, true, lerp);
			}
			CharacterHoldingHandsComponent follower = holdingHandsRelation.Follower;
			if (follower != null)
			{
				follower.OnDeleteRelation(holdingHandsRelation.FollowerHandType, false, lerp);
			}
		}
		HoldingHandsModel model2 = this.Model;
		if (model2 == null)
		{
			return;
		}
		model2.DeleteRelation(key);
	}

	// Token: 0x0600E8EE RID: 59630 RVA: 0x003F0750 File Offset: 0x003EE950
	public unsafe void RequestHoldHands(string key, EntityHandle leaderHandle, EntityHandle followerHandle, EHandType leaderHandType, bool? waitAnim, bool waitServer = true, string reason = "")
	{
		Log instance = Singleton<Log>.Instance;
		ELogModule module = ELogModule.Character;
		ELogAuthor author = ELogAuthor.LJF;
		string message = "[HoldingHandsController.RequestHoldHands]";
		<>y__InlineArray7<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray7<ValueTuple<string, object>>);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("key", key);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("leader", leaderHandle.Id);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("follower", followerHandle.Id);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 3) = new ValueTuple<string, object>("leaderHandType", leaderHandType);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 4) = new ValueTuple<string, object>("waitAnim", waitAnim);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 5) = new ValueTuple<string, object>("waitServer", waitServer);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 6) = new ValueTuple<string, object>("reason", reason);
		instance.Info(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray7<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 7));
		WorldEntity entity = leaderHandle.Entity;
		CharacterHoldingHandsComponent characterHoldingHandsComponent = (entity != null) ? entity.GetComponent<CharacterHoldingHandsComponent>() : null;
		WorldEntity entity2 = followerHandle.Entity;
		CharacterHoldingHandsComponent characterHoldingHandsComponent2 = (entity2 != null) ? entity2.GetComponent<CharacterHoldingHandsComponent>() : null;
		if (characterHoldingHandsComponent == null)
		{
			Singleton<Log>.Instance.Error(ELogModule.Character, ELogAuthor.LJF, "[HoldingHandsController.RequestHoldHands] 无法获取牵手发起者组件", default(ReadOnlySpan<ValueTuple<string, object>>));
			return;
		}
		if (characterHoldingHandsComponent2 == null)
		{
			Singleton<Log>.Instance.Error(ELogModule.Character, ELogAuthor.LJF, "[HoldingHandsController.RequestHoldHands] 无法获取牵手目标组件", default(ReadOnlySpan<ValueTuple<string, object>>));
			return;
		}
		HoldingHandsRelation relationByHand = characterHoldingHandsComponent.GetRelationByHand(leaderHandType);
		HoldingHandsRelation relationByHand2 = characterHoldingHandsComponent2.GetRelationByHand(EHandType.Right - (int)leaderHandType);
		bool flag = relationByHand != null && relationByHand2 != null && relationByHand == relationByHand2 && relationByHand.Leader == characterHoldingHandsComponent && relationByHand.Follower == characterHoldingHandsComponent2;
		if (relationByHand != null && relationByHand.Key != null)
		{
			this.RequestReleaseHands(relationByHand.Key, "同实体重复添加", false, !flag);
		}
		if (relationByHand2 != null && relationByHand2.Key != null)
		{
			HoldingHandsModel model = this.Model;
			if (((model != null) ? model.GetRelation(relationByHand2.Key) : null) != null)
			{
				this.RequestReleaseHands(relationByHand2.Key, "同实体重复添加", false, !flag);
			}
		}
		HoldingHandsModel model2 = this.Model;
		if (((model2 != null) ? model2.GetRelation(key) : null) != null)
		{
			this.RequestReleaseHands(key, "同Key重复添加", false, true);
		}
		if (waitAnim.GetValueOrDefault())
		{
			this.RequestInvitation(key, characterHoldingHandsComponent, characterHoldingHandsComponent2, leaderHandType, waitServer);
		}
		else
		{
			this.RequestAddBinding(key, characterHoldingHandsComponent, characterHoldingHandsComponent2, leaderHandType, true, waitServer);
		}
		SceneTeamModel instance2 = ModelBase<SceneTeamModel>.Instance;
		object obj = (instance2 != null) ? instance2.GetCurrentEntity : null;
		int id = leaderHandle.Id;
		object obj2 = obj;
		int? num = (obj2 != null) ? new int?(obj2.Id) : null;
		if (id == num.GetValueOrDefault() & num != null)
		{
			HoldHandEnterLogEvent holdHandEnterLogEvent = new HoldHandEnterLogEvent();
			holdHandEnterLogEvent.reason = reason + (waitAnim.GetValueOrDefault() ? "邀请" : "直接牵手");
			ControllerBase<LogReportController>.Instance.LogReport(holdHandEnterLogEvent);
		}
	}

	// Token: 0x0600E8EF RID: 59631 RVA: 0x003F0A14 File Offset: 0x003EEC14
	public void RequestInvitation(string key, CharacterHoldingHandsComponent leaderComp, CharacterHoldingHandsComponent followerComp, EHandType leaderHandType, bool waitServer = true)
	{
		if (leaderComp.IsSitDown())
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.Character;
			ELogAuthor author = ELogAuthor.LJF;
			string message = "[HoldingHandsController.RequestInvitation] Leader已在坐下，无法邀请";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("key", key);
			instance.Info(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return;
		}
		HoldHandRequest message2 = this.CreateRequest(leaderComp, followerComp, leaderHandType, true);
		if (waitServer)
		{
			Singleton<Net>.Instance.Call<HoldHandResponse>(ERequestMessageId.HoldHandRequest, message2, delegate(HoldHandResponse response, Net.CallbackStatus _)
			{
				if (response.ErrorCode == ErrorCode.Success)
				{
					this.AddInvitation(key, leaderComp, followerComp, leaderHandType);
					return;
				}
				Log instance2 = Singleton<Log>.Instance;
				ELogModule module2 = ELogModule.Character;
				ELogAuthor author2 = ELogAuthor.LJF;
				string message3 = "[HoldingHandsController.RequestInvitation] HoldHandRequest返回错误";
				ValueTuple<string, object> valueTuple2 = new ValueTuple<string, object>("ErrorCode", response.ErrorCode);
				instance2.Warn(module2, author2, message3, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple2));
			}, 0);
			return;
		}
		this.AddInvitation(key, leaderComp, followerComp, leaderHandType);
		Singleton<Net>.Instance.Call<HoldHandResponse>(ERequestMessageId.HoldHandRequest, message2, delegate(HoldHandResponse _, Net.CallbackStatus _)
		{
		}, 0);
	}

	// Token: 0x0600E8F0 RID: 59632 RVA: 0x003F0B10 File Offset: 0x003EED10
	public void RequestAddBinding(string key, CharacterHoldingHandsComponent leaderComp, CharacterHoldingHandsComponent followerComp, EHandType leaderHandType, bool teleport, bool waitServer = true)
	{
		Action onSuccess = delegate()
		{
			this.AddBinding(key, leaderComp, followerComp, leaderHandType, false, false).NoLerpNextUpdate = true;
			if (teleport)
			{
				global::Vector followingPosition = followerComp.GetFollowingPosition(leaderComp);
				CharacterActorComponent actorComp = followerComp.ActorComp;
				if (actorComp != null)
				{
					actorComp.TeleportAndFindStandLocation(followingPosition, true);
				}
				BaseMoveComponent moveComp = followerComp.MoveComp;
				if (moveComp != null)
				{
					MoveToLocationController moveController = moveComp.MoveController;
					if (moveController != null)
					{
						moveController.PushMoveInfo();
					}
				}
				CharacterActorComponent actorComp2 = followerComp.ActorComp;
				if (actorComp2 != null)
				{
					actorComp2.SetActorRotation(leaderComp.ActorComp.ActorRotation, "跳过动画直接牵手时传送被牵手者并设置旋转", false);
				}
				CharacterActorComponent actorComp3 = followerComp.ActorComp;
				if (actorComp3 == null)
				{
					return;
				}
				actorComp3.ClearInput(false, true);
			}
		};
		HoldHandRequest message = this.CreateRequest(leaderComp, followerComp, leaderHandType, true);
		if (waitServer)
		{
			Singleton<Net>.Instance.Call<HoldHandResponse>(ERequestMessageId.HoldHandRequest, message, delegate(HoldHandResponse response, Net.CallbackStatus _)
			{
				if (response.ErrorCode == ErrorCode.Success)
				{
					onSuccess();
					return;
				}
				Log instance = Singleton<Log>.Instance;
				ELogModule module = ELogModule.Character;
				ELogAuthor author = ELogAuthor.LJF;
				string message2 = "[HoldingHandsController.RequestAddBinding] HoldHandRequest返回错误";
				ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("ErrorCode", response.ErrorCode);
				instance.Warn(module, author, message2, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			}, 0);
			return;
		}
		onSuccess();
		Singleton<Net>.Instance.Call<HoldHandResponse>(ERequestMessageId.HoldHandRequest, message, delegate(HoldHandResponse _, Net.CallbackStatus _)
		{
		}, 0);
	}

	// Token: 0x0600E8F1 RID: 59633 RVA: 0x003F0BD8 File Offset: 0x003EEDD8
	public unsafe void RequestReleaseHands(string key, string reason = "", bool lerp = false, bool sendRequest = true)
	{
		HoldingHandsModel model = this.Model;
		HoldingHandsRelation holdingHandsRelation = (model != null) ? model.GetRelation(key) : null;
		if (holdingHandsRelation == null)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.Character;
			ELogAuthor author = ELogAuthor.LJF;
			string message = "[HoldingHandsController.RequestReleaseHands] key不存在";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("key", key);
			instance.Warn(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return;
		}
		CharacterHoldingHandsComponent leader = holdingHandsRelation.Leader;
		CharacterHoldingHandsComponent follower = holdingHandsRelation.Follower;
		if (leader == null || follower == null)
		{
			return;
		}
		this.DeleteRelation(key, lerp);
		if (sendRequest)
		{
			HoldHandRequest message2 = this.CreateRequest(leader, follower, holdingHandsRelation.LeaderHandType, false);
			Singleton<Net>.Instance.Call<HoldHandResponse>(ERequestMessageId.HoldHandRequest, message2, delegate(HoldHandResponse _, Net.CallbackStatus _)
			{
			}, 0);
			SceneTeamModel instance2 = ModelBase<SceneTeamModel>.Instance;
			object obj = (instance2 != null) ? instance2.GetCurrentEntity : null;
			int id = leader.Entity.Id;
			object obj2 = obj;
			int? num = (obj2 != null) ? new int?(obj2.Id) : null;
			if (id == num.GetValueOrDefault() & num != null)
			{
				HoldHandExitLogEvent holdHandExitLogEvent = new HoldHandExitLogEvent();
				holdHandExitLogEvent.reason = reason;
				ControllerBase<LogReportController>.Instance.LogReport(holdHandExitLogEvent);
			}
		}
		Log instance3 = Singleton<Log>.Instance;
		ELogModule module2 = ELogModule.Character;
		ELogAuthor author2 = ELogAuthor.LJF;
		string message3 = "[HoldingHandsController.RequestReleaseHands]";
		<>y__InlineArray5<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray5<ValueTuple<string, object>>);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("key", key);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("reason", reason);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("sendRequest", sendRequest);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 3) = new ValueTuple<string, object>("leader", leader.Entity.Id);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 4) = new ValueTuple<string, object>("follower", follower.Entity.Id);
		instance3.Info(module2, author2, message3, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray5<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 5));
	}

	// Token: 0x0600E8F2 RID: 59634 RVA: 0x003F0DB4 File Offset: 0x003EEFB4
	private HoldHandRequest CreateRequest(CharacterHoldingHandsComponent leaderComp, CharacterHoldingHandsComponent followerComp, EHandType leaderHandType, bool isHold)
	{
		HoldHandRequest holdHandRequest = HoldHandRequest.Create();
		holdHandRequest.HostPlayerId = ModelBase<CreatureModel>.Instance.GetWorldOwner();
		holdHandRequest.EntityId = leaderComp.ActorComp.CreatureData.GetCreatureDataId();
		holdHandRequest.TargetEntityId = followerComp.ActorComp.CreatureData.GetCreatureDataId();
		holdHandRequest.HandType = (int)leaderHandType;
		holdHandRequest.IsHold = isHold;
		holdHandRequest.ActionType = 0;
		return holdHandRequest;
	}

	// Token: 0x0600E8F3 RID: 59635 RVA: 0x003F0E18 File Offset: 0x003EF018
	private void OnStopHoldHandNotify(StopHoldHandNotify message, [Nullable(2)] Net.CallbackStatus _)
	{
		long num = Singleton<MathUtils>.Instance.LongToNumber(message.EntityId);
		EntityHandle entity = ModelBase<CreatureModel>.Instance.GetEntity(num);
		if (entity == null || !entity.Valid)
		{
			Log instance = Singleton<Log>.Instance;
			ELogModule module = ELogModule.Character;
			ELogAuthor author = ELogAuthor.LJF;
			string message2 = "[HoldingHandsController.OnStopHoldHandNotify] 找不到对应的Entity";
			ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("id", num);
			instance.Error(module, author, message2, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
			return;
		}
		WorldEntity entity2 = entity.Entity;
		CharacterHoldingHandsComponent characterHoldingHandsComponent = (entity2 != null) ? entity2.GetComponent<CharacterHoldingHandsComponent>() : null;
		if (characterHoldingHandsComponent == null)
		{
			Log instance2 = Singleton<Log>.Instance;
			ELogModule module2 = ELogModule.Character;
			ELogAuthor author2 = ELogAuthor.LJF;
			string message3 = "[HoldingHandsController.OnStopHoldHandNotify] Entity没有牵手Component";
			ValueTuple<string, object> valueTuple2 = new ValueTuple<string, object>("id", num);
			instance2.Error(module2, author2, message3, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple2));
			return;
		}
		Log instance3 = Singleton<Log>.Instance;
		ELogModule module3 = ELogModule.Character;
		ELogAuthor author3 = ELogAuthor.LJF;
		string message4 = "[HoldingHandsController.OnStopHoldHandNotify]";
		ValueTuple<string, object> valueTuple3 = new ValueTuple<string, object>("leader", characterHoldingHandsComponent.Entity.Id);
		instance3.Info(module3, author3, message4, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple3));
		characterHoldingHandsComponent.ReleaseAllHands("Notify", false, false);
	}

	// Token: 0x0600E8F4 RID: 59636 RVA: 0x003F0F08 File Offset: 0x003EF108
	public unsafe void SitOnCharCheckHoldHands(Entity leader, BaseActorComponent sceneItem)
	{
		CharacterHoldingHandsComponent component = leader.GetComponent<CharacterHoldingHandsComponent>();
		if (sceneItem == null || ((component != null) ? component.ActorComp : null) == null)
		{
			return;
		}
		HoldingHandsRelation holdingHandsRelation = (component != null) ? component.OnLeaderSitDown() : null;
		CharacterHoldingHandsComponent characterHoldingHandsComponent = (holdingHandsRelation != null) ? holdingHandsRelation.Follower : null;
		if (((characterHoldingHandsComponent != null) ? characterHoldingHandsComponent.ActorComp : null) == null)
		{
			return;
		}
		global::Vector actorLocationProxy = component.ActorComp.ActorLocationProxy;
		global::Vector actorLocationProxy2 = characterHoldingHandsComponent.ActorComp.ActorLocationProxy;
		IEntityData pbEntityInitData = sceneItem.CreatureData.GetPbEntityInitData();
		Dictionary<EConfigComponent, IComponentBase> dictionary = (pbEntityInitData != null) ? pbEntityInitData.ComponentsData : null;
		int pbDataId = sceneItem.CreatureData.GetPbDataId();
		IEntityOwnerInfo entityOwner = ModelBase<CreatureModel>.Instance.GetEntityOwner(ModelBase<GameModeModel>.Instance.MapConfig.MapId, pbDataId, true);
		int num = 0;
		if (entityOwner != null && entityOwner != null && entityOwner.Type == EEntityOwnerType.Entity)
		{
			IEntityOwnerEntity entityOwnerEntity = entityOwner as IEntityOwnerEntity;
			num = ((entityOwnerEntity != null) ? entityOwnerEntity.EntityId : 0);
		}
		IEntityData entityData = null;
		if (num != 0)
		{
			entityData = ModelBase<CreatureModel>.Instance.GetCompleteEntityData(new int?(num), null);
		}
		int num2 = -1;
		double num3 = 500000.0;
		List<int> list = new List<int>();
		if (entityData != null)
		{
			BaseInfoComponent component2 = TdUtils.GetComponent<BaseInfoComponent>(entityData.ComponentsData, EConfigComponent.BaseInfoComponent);
			if (((component2 != null) ? component2.ChildEntityIds : null) != null)
			{
				list.Add(num);
				foreach (int item in component2.ChildEntityIds)
				{
					list.Add(item);
				}
			}
		}
		if (dictionary != null)
		{
			BaseInfoComponent component3 = TdUtils.GetComponent<BaseInfoComponent>(dictionary, EConfigComponent.BaseInfoComponent);
			if (((component3 != null) ? component3.ChildEntityIds : null) != null)
			{
				foreach (int item2 in component3.ChildEntityIds)
				{
					list.Add(item2);
				}
			}
		}
		foreach (int num4 in list)
		{
			ModelBase<CreatureModel>.Instance.GetEntitiesWithPbDataId(num4, ref this.EntityHandles);
			EntityHandle entityHandle = this.EntityHandles[0];
			PawnInteractNewComponent pawnInteractNewComponent;
			if (entityHandle == null)
			{
				pawnInteractNewComponent = null;
			}
			else
			{
				WorldEntity entity = entityHandle.Entity;
				pawnInteractNewComponent = ((entity != null) ? entity.GetComponent<PawnInteractNewComponent>() : null);
			}
			PawnInteractNewComponent pawnInteractNewComponent2 = pawnInteractNewComponent;
			global::Vector vector = (pawnInteractNewComponent2 != null) ? pawnInteractNewComponent2.GetInteractPoint() : null;
			if (vector != null && pawnInteractNewComponent2 != null && pawnInteractNewComponent2.HasSitDownAction() && pawnInteractNewComponent2.Entity.Id != sceneItem.Entity.Id)
			{
				double num5 = global::Vector.DistSquared(vector, actorLocationProxy);
				double num6 = global::Vector.DistSquared(vector, actorLocationProxy2);
				double num7 = num5 + num6;
				if (num7 < num3)
				{
					num3 = num7;
					num2 = num4;
				}
			}
		}
		if (num2 > 0)
		{
			this.FollowerSitOnChairAction(component, num2, characterHoldingHandsComponent);
		}
		SceneTeamModel instance = ModelBase<SceneTeamModel>.Instance;
		object obj = (instance != null) ? instance.GetCurrentEntity : null;
		int id = component.Entity.Id;
		object obj2 = obj;
		int? num8 = (obj2 != null) ? new int?(obj2.Id) : null;
		if (id == num8.GetValueOrDefault() & num8 != null)
		{
			HoldHandSitDownLogEvent logData = new HoldHandSitDownLogEvent();
			ControllerBase<LogReportController>.Instance.LogReport(logData);
		}
		Log instance2 = Singleton<Log>.Instance;
		ELogModule module = ELogModule.Character;
		ELogAuthor author = ELogAuthor.LJF;
		string message = "[HoldingHandsController] 牵手坐下";
		<>y__InlineArray3<ValueTuple<string, object>> <>y__InlineArray = default(<>y__InlineArray3<ValueTuple<string, object>>);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 0) = new ValueTuple<string, object>("key", (holdingHandsRelation != null) ? holdingHandsRelation.Key : null);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 1) = new ValueTuple<string, object>("leader", component.Entity.Id);
		*<PrivateImplementationDetails>.InlineArrayElementRef<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(ref <>y__InlineArray, 2) = new ValueTuple<string, object>("follower", characterHoldingHandsComponent.Entity.Id);
		instance2.Info(module, author, message, <PrivateImplementationDetails>.InlineArrayAsReadOnlySpan<<>y__InlineArray3<ValueTuple<string, object>>, ValueTuple<string, object>>(<>y__InlineArray, 3));
	}

	// Token: 0x0600E8F5 RID: 59637 RVA: 0x003F12DC File Offset: 0x003EF4DC
	private void FollowerSitOnChairAction(CharacterHoldingHandsComponent leaderComp, int chairId, CharacterHoldingHandsComponent followerComp)
	{
		ModelBase<CreatureModel>.Instance.GetEntitiesWithPbDataId(chairId, ref this.EntityHandles);
		EntityHandle entityHandle = this.EntityHandles[0];
		if (entityHandle == null)
		{
			return;
		}
		WorldEntity entity = entityHandle.Entity;
		PawnInteractNewComponent pawnInteractNewComponent = (entity != null) ? entity.GetComponent<PawnInteractNewComponent>() : null;
		if (pawnInteractNewComponent != null && pawnInteractNewComponent.HasSitDownAction())
		{
			Action action = delegate()
			{
				if (!leaderComp.IsSitDown())
				{
					leaderComp.OnLeaderAndFollowerStandUp();
				}
			};
			NpcSitOnChairComponent npcSitOnChairComponent = (followerComp != null) ? followerComp.Entity.GetComponent<NpcSitOnChairComponent>() : null;
			TArray<int> tarray;
			if (followerComp == null)
			{
				tarray = null;
			}
			else
			{
				BP_KeepFollowingConfig_C keepFollowingConfig = followerComp.KeepFollowingConfig;
				tarray = ((keepFollowingConfig != null) ? keepFollowingConfig.传送特效buffID : null);
			}
			TArray<int> tarray2 = tarray;
			List<int> list = new List<int>();
			int? num = (tarray2 != null) ? new int?(tarray2.Num()) : null;
			if (num != null && num.Value != 0)
			{
				for (int i = 0; i < num.Value; i++)
				{
					int num2 = tarray2.Get(i);
					if (num2 != 0 && !list.Contains(num2))
					{
						list.Add(num2);
					}
				}
			}
			if (npcSitOnChairComponent != null)
			{
				npcSitOnChairComponent.StartNpcSitOnChair(new NpcSitOnChairParams
				{
					ChairEntityId = chairId,
					TeleportEffect = list.ToArray(),
					InterruptCondition = delegate
					{
						if (leaderComp == null)
						{
							return true;
						}
						CharacterActionComponent component = leaderComp.Entity.GetComponent<CharacterActionComponent>();
						return (component == null || !component.GetSitDownState()) && !ModelBase<PlotModel>.Instance.IsInPlot;
					},
					Finish = action,
					Abort = action
				});
			}
		}
	}

	// Token: 0x0600E8F6 RID: 59638 RVA: 0x003F142A File Offset: 0x003EF62A
	public void ForceNoLerpNextUpdate(EntityHandle entityHandle)
	{
		WorldEntity entity = entityHandle.Entity;
		CharacterHoldingHandsComponent characterHoldingHandsComponent = (entity != null) ? entity.GetComponent<CharacterHoldingHandsComponent>() : null;
		if (characterHoldingHandsComponent == null)
		{
			return;
		}
		characterHoldingHandsComponent.SetBindingsNoLerp();
	}

	// Token: 0x0600E8F7 RID: 59639 RVA: 0x003F1448 File Offset: 0x003EF648
	[return: Nullable(new byte[]
	{
		2,
		1
	})]
	public List<EntityHandle> GetFollowers(EntityHandle leaderHandle)
	{
		WorldEntity entity = leaderHandle.Entity;
		CharacterHoldingHandsComponent characterHoldingHandsComponent = (entity != null) ? entity.GetComponent<CharacterHoldingHandsComponent>() : null;
		if (characterHoldingHandsComponent == null)
		{
			return null;
		}
		List<EntityHandle> list = new List<EntityHandle>();
		for (int i = 0; i <= 1; i++)
		{
			HoldingHandsRelation relationByHand = characterHoldingHandsComponent.GetRelationByHand((EHandType)i);
			if (relationByHand != null && relationByHand.Leader.Entity.Id == leaderHandle.Id)
			{
				list.Add(ModelBase<CreatureModel>.Instance.GetEntityById(relationByHand.Follower.Entity.Id));
			}
		}
		return list;
	}

	// Token: 0x04007035 RID: 28725
	private List<EntityHandle> EntityHandles = new List<EntityHandle>();
}
