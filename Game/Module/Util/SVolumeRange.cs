using System;
using System.Runtime.CompilerServices;
using Aki.TDConfigMgr.AutoGenerated.IEntity;
using Aki.TDConfigMgr.AutoGenerated.IShape;
using UnrealEngine;

namespace CSharpScript.Game.Module.Util
{
	// Token: 0x02004C6C RID: 19564
	[NullableContext(1)]
	[Nullable(0)]
	internal sealed class SVolumeRange : SRange
	{
		// Token: 0x06032F8E RID: 208782 RVA: 0x00CC4990 File Offset: 0x00CC2B90
		public override bool CheckPositionReached(Vector pos)
		{
			if (StringUtils.IsBlank(this.VolumeKey))
			{
				Log instance = Singleton<Log>.Instance;
				ELogModule module = ELogModule.Controller;
				ELogAuthor author = ELogAuthor.XDW;
				DefaultInterpolatedStringHandler defaultInterpolatedStringHandler = new DefaultInterpolatedStringHandler(69, 1);
				defaultInterpolatedStringHandler.AppendLiteral("[RangeCheck] CheckReachedPosition Volume 但是 entityPbId:");
				defaultInterpolatedStringHandler.AppendFormatted<int>(this.EntityPbId);
				defaultInterpolatedStringHandler.AppendLiteral(" 没有配置VolumeKey");
				instance.Error(module, author, defaultInterpolatedStringHandler.ToStringAndClear(), default(ReadOnlySpan<ValueTuple<string, object>>));
				return false;
			}
			UKuroTriggerVolumeManager ukuroTriggerVolumeManager = UKuroRenderingRuntimeBPPluginBPLibrary.GetSubsystem(GlobalData.World, UKuroTriggerVolumeManager.StaticClass()) as UKuroTriggerVolumeManager;
			if (ukuroTriggerVolumeManager == null)
			{
				Singleton<Log>.Instance.Error(ELogModule.Controller, ELogAuthor.XDW, "[RangeCheck] CheckReachedPosition Volume 但是KuroTriggerVolumeManager无效", default(ReadOnlySpan<ValueTuple<string, object>>));
				return false;
			}
			AActor kuroTriggerVolume = ukuroTriggerVolumeManager.GetKuroTriggerVolume(FNameUtil.GetDynamicFName(this.VolumeKey).Value);
			if (kuroTriggerVolume == null)
			{
				Log instance2 = Singleton<Log>.Instance;
				ELogModule module2 = ELogModule.Controller;
				ELogAuthor author2 = ELogAuthor.XDW;
				DefaultInterpolatedStringHandler defaultInterpolatedStringHandler = new DefaultInterpolatedStringHandler(47, 2);
				defaultInterpolatedStringHandler.AppendLiteral("[RangeCheck] CheckReachedPosition Volume 但是");
				defaultInterpolatedStringHandler.AppendFormatted<int>(this.EntityPbId);
				defaultInterpolatedStringHandler.AppendLiteral(":");
				defaultInterpolatedStringHandler.AppendFormatted(this.VolumeKey);
				defaultInterpolatedStringHandler.AppendLiteral("不存在");
				instance2.Warn(module2, author2, defaultInterpolatedStringHandler.ToStringAndClear(), default(ReadOnlySpan<ValueTuple<string, object>>));
				return false;
			}
			ActorSystem instance3 = Singleton<ActorSystem>.Instance;
			UClassStackOnlyPtr ueClassPtr = ATriggerSphere.StaticClass();
			FVectorDouble fvectorDouble = pos.ToUeVector(false);
			AActor aactor = instance3.Get(ueClassPtr, new FTransformDouble(ref fvectorDouble), null, true);
			if (aactor == null)
			{
				return false;
			}
			TArray<AActor> tarray = new TArray<AActor>();
			kuroTriggerVolume.GetOverlappingActors(ref tarray, default(TSubclassOf<AActor>));
			bool result = tarray.FindIndex(aactor) != -1;
			Singleton<ActorSystem>.Instance.Put("CheckReachedPosition Volume", aactor, null);
			return result;
		}

		// Token: 0x06032F8F RID: 208783 RVA: 0x00CC4B28 File Offset: 0x00CC2D28
		public override bool MakeRange(IEntityData entityData, ITriggerShape shapeConfig)
		{
			IVolumeTriggerShape volumeTriggerShape = shapeConfig as IVolumeTriggerShape;
			if (volumeTriggerShape == null)
			{
				return false;
			}
			this.VolumeKey = volumeTriggerShape.VolumeKey;
			this.EntityPbId = entityData.Id;
			return true;
		}

		// Token: 0x0401DA8C RID: 121484
		[Nullable(2)]
		public string VolumeKey;

		// Token: 0x0401DA8D RID: 121485
		private int EntityPbId;
	}
}
