using System;
using System.Runtime.CompilerServices;
using Aki.TDConfigMgr.AutoGenerated.ICondition;
using UnrealEngine;

namespace CSharpScript.Game.LevelGamePlay
{
	// Token: 0x02006A00 RID: 27136
	public class LevelConditionCheckPlayerRelativeDistance : LevelConditionBase
	{
		// Token: 0x060433BE RID: 275390 RVA: 0x0114965C File Offset: 0x0114785C
		[NullableContext(1)]
		public override bool CheckNew(ICondition2 inConditionInfo, AActor inTrigger, params object[] eventArgs)
		{
			ICheckPlayerRelativeDistance checkPlayerRelativeDistance = inConditionInfo as ICheckPlayerRelativeDistance;
			if (checkPlayerRelativeDistance == null)
			{
				return false;
			}
			int targetEntityId = checkPlayerRelativeDistance.TargetEntityId;
			EntityHandle entityByPbDataId = ModelBase<CreatureModel>.Instance.GetEntityByPbDataId(targetEntityId);
			WorldEntity worldEntity = (entityByPbDataId != null) ? entityByPbDataId.Entity : null;
			if (worldEntity == null || !worldEntity.Valid)
			{
				Log instance = Singleton<Log>.Instance;
				ELogModule module = ELogModule.LevelCondition;
				ELogAuthor author = ELogAuthor.CH;
				string message = "LevelConditionCheckPlayerRelativeDistance: 未找到目标实体";
				ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("pbDataId", targetEntityId);
				instance.Warn(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
				return false;
			}
			BaseActorComponent component = worldEntity.GetComponent<BaseActorComponent>();
			if (component == null)
			{
				Log instance2 = Singleton<Log>.Instance;
				ELogModule module2 = ELogModule.LevelCondition;
				ELogAuthor author2 = ELogAuthor.CH;
				string message2 = "LevelConditionCheckPlayerRelativeDistance: 目标实体缺少BaseActorComponent组件";
				ValueTuple<string, object> valueTuple2 = new ValueTuple<string, object>("pbDataId", targetEntityId);
				instance2.Warn(module2, author2, message2, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple2));
				return false;
			}
			Vector v = Vector.Create(component.ActorLocationProxy);
			SceneTeamModel instance3 = ModelBase<SceneTeamModel>.Instance;
			WorldEntity worldEntity2;
			if (instance3 == null)
			{
				worldEntity2 = null;
			}
			else
			{
				EntityHandle getCurrentEntity = instance3.GetCurrentEntity;
				worldEntity2 = ((getCurrentEntity != null) ? getCurrentEntity.Entity : null);
			}
			WorldEntity worldEntity3 = worldEntity2;
			if (worldEntity3 == null)
			{
				Singleton<Log>.Instance.Warn(ELogModule.LevelCondition, ELogAuthor.CH, "LevelConditionCheckPlayerRelativeDistance: 未找到当前角色实体", default(ReadOnlySpan<ValueTuple<string, object>>));
				return false;
			}
			BaseActorComponent component2 = worldEntity3.GetComponent<BaseActorComponent>();
			if (component2 == null)
			{
				Singleton<Log>.Instance.Warn(ELogModule.LevelCondition, ELogAuthor.CH, "LevelConditionCheckPlayerRelativeDistance: 当前角色实体缺少BaseActorComponent组件", default(ReadOnlySpan<ValueTuple<string, object>>));
				return false;
			}
			double num = Vector.Distance(Vector.Create(component2.ActorLocationProxy), v);
			foreach (IDistanceRange distanceRange in checkPlayerRelativeDistance.ValidDistanceRanges)
			{
				if (distanceRange.Min != null && num < (double)distanceRange.Min.Value)
				{
					return false;
				}
				if (distanceRange.Max != null && num > (double)distanceRange.Max.Value)
				{
					return false;
				}
			}
			return true;
		}
	}
}
