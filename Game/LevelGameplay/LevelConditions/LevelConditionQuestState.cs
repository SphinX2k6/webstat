using System;
using System.Runtime.CompilerServices;
using Aki.TDConfigMgr.AutoGenerated.ICondition;
using Aki.TDConfigMgr.AutoGenerated.IVar;
using CSharpScript.Game.Module.RecallQuest.Model;
using UnrealEngine;

namespace CSharpScript.Game.LevelGamePlay.LevelConditions
{
	// Token: 0x02006DD8 RID: 28120
	public class LevelConditionQuestState : LevelConditionBase
	{
		// Token: 0x060445F9 RID: 280057 RVA: 0x011C31F4 File Offset: 0x011C13F4
		[NullableContext(1)]
		public override bool CheckNew(ICondition2 inConditionInfo, [Nullable(2)] AActor inTrigger, params object[] eventArgs)
		{
			IQuestStateEqualCondition questStateEqualCondition = inConditionInfo as IQuestStateEqualCondition;
			if (questStateEqualCondition == null)
			{
				return false;
			}
			int questId = ModelBase<RecallQuestModel>.Instance.TryGetRecallQuestIdByQuestId(questStateEqualCondition.QuestId);
			EQuestState questState = ModelBase<QuestNewModel>.Instance.GetQuestState(questId);
			bool result = false;
			switch (questStateEqualCondition.Compare)
			{
			case ECompare.Eq:
				result = (questState == questStateEqualCondition.State);
				break;
			case ECompare.Ne:
				result = (questState != questStateEqualCondition.State);
				break;
			case ECompare.Ge:
				result = (questState >= questStateEqualCondition.State);
				break;
			case ECompare.Gt:
				result = (questState > questStateEqualCondition.State);
				break;
			case ECompare.Le:
				result = (questState <= questStateEqualCondition.State);
				break;
			case ECompare.Lt:
				result = (questState < questStateEqualCondition.State);
				break;
			}
			return result;
		}
	}
}
