using System;
using System.Runtime.CompilerServices;
using Aki.Config;
using Aki.TDConfigMgr.AutoGenerated.IEntity;
using UnrealEngine;

namespace CSharpScript.Game.LevelGamePlay.LevelConditions
{
	// Token: 0x02006DA6 RID: 28070
	public class LevelConditionPawnInRange : LevelConditionBase
	{
		// Token: 0x06044594 RID: 279956 RVA: 0x011C2310 File Offset: 0x011C0510
		[NullableContext(1)]
		public override bool Check(Condition inConditionInfo, [Nullable(2)] AActor inTrigger, params object[] eventArgs)
		{
			if (eventArgs != null && eventArgs.Length == 0)
			{
				return false;
			}
			if (inConditionInfo.LimitParamsLength == 0)
			{
				Log instance = Singleton<Log>.Instance;
				ELogModule module = ELogModule.LevelCondition;
				ELogAuthor author = ELogAuthor.TL;
				string message = "配置错误！条件的参数不应该为空";
				ValueTuple<string, object> valueTuple = new ValueTuple<string, object>("inConditionInfo.Id", inConditionInfo.Id);
				instance.Error(module, author, message, new ReadOnlySpan<ValueTuple<string, object>>(ref valueTuple));
				return false;
			}
			string limitParams = inConditionInfo.GetLimitParams("PawnId");
			int id = (int)eventArgs[0];
			Entity entity = Singleton<EntitySystem>.Instance.Get(id);
			if (entity == null || !entity.Active)
			{
				return false;
			}
			CreatureDataComponent creatureDataComponent = (entity != null) ? entity.GetComponent<CreatureDataComponent>() : null;
			if (creatureDataComponent != null && creatureDataComponent.IsConcealed)
			{
				return false;
			}
			string text;
			if (creatureDataComponent == null)
			{
				text = null;
			}
			else
			{
				IEntityData pbEntityInitData = creatureDataComponent.GetPbEntityInitData();
				text = ((pbEntityInitData != null) ? pbEntityInitData.BlueprintType : null);
			}
			string text2 = text;
			return text2 != null && !(text2 != limitParams) && entity.GetComponent<BaseActorComponent>() != null && Global.BaseCharacter != null && Global.BaseCharacter.CharacterActorComponent != null;
		}
	}
}
